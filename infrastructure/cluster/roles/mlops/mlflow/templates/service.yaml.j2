apiVersion: v1
kind: Service
metadata:
  name: mlflow
  namespace: {{ mlflow_namespace }}
spec:
  type: NodePort
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: {{ mlflow_nodeport }}
  selector:
    app: mlflow