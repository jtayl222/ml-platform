mirrors:
  docker.io:
    endpoint:
      - "http://{{ harbor_loadbalancer_ip | default('192.168.1.210') }}"
  "{{ harbor_loadbalancer_ip | default('192.168.1.210') }}":
    endpoint:
      - "http://{{ harbor_loadbalancer_ip | default('192.168.1.210') }}"

configs:
  "{{ harbor_loadbalancer_ip | default('192.168.1.210') }}":
    auth:
      username: "{{ harbor_admin_user | default('admin') }}"
      password: "{{ harbor_admin_password | default('Harbor12345') }}"
    tls:
      insecure_skip_verify: true